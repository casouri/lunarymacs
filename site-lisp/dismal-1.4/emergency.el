  ;; --- save contents of *cmushell* buffer:
  ;; (save-excursion
  ;;(set-buffer \"*cmushell*\")
  ;;(auto-save-mode 1)
  ;;(do-auto-save))

  ;; ...then exit emacs:
  ;; (kill-emacs t)
(setq debug-on-error t)
(+ 3 'adam)
