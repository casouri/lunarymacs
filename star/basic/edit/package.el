;;; -*- lexical-binding: t -*-

(package| expand-region
          undo-tree
          avy
          embrace
	  recentf-ext
          smooth-scrolling
          minimap
          goto-last-change
          avy)
