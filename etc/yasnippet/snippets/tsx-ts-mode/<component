# -*- mode: snippet -*-
# name: <component
# key: <component
# --

import React from 'react';

type Props = {

};

const `(setq my-snippet--component-name (car (string-split (file-name-nondirectory (buffer-file-name)) "\\.")))` = ({}: Props) => {
    return (null);
}

export default `my-snippet--component-name`;