;;(package-initialize t)

(load (concat (expand-file-name user-emacs-directory) "core/core"))


(moon|
      :basic
      key
      evil
      basic-ui
      ui
      other
      edit
      :autocomplete
      ivy
      company
      :casouri
      casouri
      :os
      mac
      )
