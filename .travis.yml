os: osx
language: emacs-lisp
sudo: false

install:
  - git clone https://github.com/casouri/lunarymacs.git .emacs.d --recurse-submodules
  - make install-emacs25
  - cd .emacs.d
  - cp init.example.el init.el

script:
  - make

